stages:
  - build
  - test
  - sonarqube

build:
  stage: build
  script:
    - ./gradlew assemble
 
test:
  stage: test
  dependencies:
    - build
  script:
    - ./gradlew test

sonarqube:
  stage: sonarqube
  dependencies:
    - test
  script:
    - ./gradlew sonarqube
    
after_script:
  - echo "End CI"
